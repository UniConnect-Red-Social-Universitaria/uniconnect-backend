/// <reference types="jest" />
import { describe, it } from '@jest/globals';

describe('EmailStrategy', () => {
  it.todo('los casos de EmailInstitucionalStrategy están cubiertos en notificacion-strategy.test.ts');
});